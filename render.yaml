services:
  - type: web
    name: vivasense-backend
    runtime: python
    buildCommand: pip install -r app/requirements.txt
    startCommand: python -X utf8 app/main.py --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHONPATH
        value: /opt/render/project/src/app
